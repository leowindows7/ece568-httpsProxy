#include <unistd.h>

#include <cstdlib>

void create_daemon() {
  pid_t pid = fork();
  if (pid < 0) {
    // TODO: refactor this to throw exception
    exit(EXIT_FAILURE);
  }

  if (pid != 0) {
    exit(EXIT_SUCCESS);
  }  // exit parent

  if (setsid() < 0) {
  }
}

int main() {
}
